# @clarity-agent/mcp

> **Not yet available.** Requires the `clarity-agent` Python package on PyPI. Use the Python server directly until then. See [`src/clarity_agent/mcp/README.md`](../src/clarity_agent/mcp/README.md).

Once published:

```bash
npx @clarity-agent/mcp --project-dir /path/to/project